# Build Resources

Place your `icon.ico` file here for the installer.

Requirements:
- `icon.ico` — Windows icon file (256x256 recommended, multi-size)
- You can convert a PNG to ICO using online tools or ImageMagick

If you don't have an icon, the build will still work but use a default Electron icon.
